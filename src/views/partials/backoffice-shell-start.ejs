<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %> - <%= appName %></title>
    <link rel="stylesheet" href="/dashboard-assets/css/main.css?v=<%= Date.now() %>" />
    <link rel="stylesheet" href="/css/main.css?v=<%= Date.now() %>" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  </head>
  <body class="app-body">
    <%
      const resolvedCurrentUser = currentUser || null;
      const resolvedCurrentRole = resolvedCurrentUser?.primaryRole?.display_name || resolvedCurrentUser?.primaryRole?.name || resolvedCurrentUser?.role || '-';
    %>
    <div class="app-shell">
      <%- include('../../dashboard/src/views/partials/sidebar', {
        activePage,
        currentUser: resolvedCurrentUser,
        currentRole: resolvedCurrentRole,
        appName,
        csrfToken
      }) %>
      <div class="main-column">
        <%- include('../../dashboard/src/views/partials/topbar', { pageTitle }) %>
        <main class="page-content">
